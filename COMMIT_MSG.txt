Step SKIP passthrough (5.2, Batch F1)

A step can be marked SKIP. A SKIP step is not executed; it passes its input
through to its output unchanged and the run continues.

Semantics: "copy input to output as-is" = copy every file (recursively) from the
PREVIOUS step's output directory into this step's output directory; if the SKIP
step is the first step the source is 00_landing_in. Recorded as SKIPPED with a
"copied N file(s)" message. (Flagged in the spec for confirmation in case a
different input source is intended.)

- Model: StepDef.skip; XML attribute skip="true" (mirrors overwrite) round-trips
  through parser, writer, WorkflowDto.NodeDto.skip and toDto.
- Designer: a "Step mode" dropdown (normal / skip (passthrough)) in the common
  step fields; serialised as skip="true".
- Engine (executeStep): before running, if step.skip, copyDirContents(prev, dst),
  mark StepStatus.SKIPPED, audit STEP_SKIPPED, continue; a copy failure FAILs the
  step so a broken passthrough never silently drops data. copyDirContents helper
  added (recursive copy, overwrite).

Docs: .claude/2026-07-15-step-skip-passthrough-F1.md and CLAUDE.md.

Verify: copyDirContents run standalone (nested files, overwrite, subdirs; 3/3
copied); skip attribute round-trip verified via DOM. designer.html passes
node --check (no literal \n/\r, no unsafe Thymeleaf). Round-trip mirrors the proven
overwrite attribute. Full Maven build to be confirmed on deploy.
