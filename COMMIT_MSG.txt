Operations grid: search-all, source/target descriptions, tags (1.1-1.3)

1.1 The feed filter now matches across every displayed field: feedId, name,
    source id + source description, target id + target description, tags, last
    status, failed step, last-run and last-success timestamps (dates are now
    searchable), and the output-data labels/values.
1.2 The SOURCE and TARGET columns show the id (mono) with the description beneath
    it. Both descriptions were already returned by /api/overview/feeds; display
    change only.
1.3 /api/overview/feeds now also returns tags (from the workflow definition). The
    FEED column shows a "tags: ..." line and tags are included in the search.

CSV export/copy updated to include sourceDescription, targetDescription and tags.

Docs: .claude/2026-07-15-operations-grid-search-desc-tags.md and CLAUDE.md.

Verify: one backend line added (tags) mirroring the existing targetDescription
put; overview.html passes node --check (no literal \n/\r, no unsafe Thymeleaf).
The search hay-building was exercised in Node -- matches a date, a tag, the source
and target descriptions, and an output-data label/value; rejects an absent string.
Full render to be confirmed on deploy.
