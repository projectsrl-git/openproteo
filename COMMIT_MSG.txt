CSV write speed-up; Operations resources + clickable rollup + target/last-run

G2 - faster ResultSet->CSV writing (output byte-identical):
- CsvWriter: skip UTF-8 byte counting unless splitting by MB (a full per-row
  pass removed in the common case); reuse one StringBuilder + char[] across rows
  (no per-row String/StringBuilder allocation); single-pass field quoting; 512 KB
  output buffer. Round-trip self-test confirms identical bytes.
- SqlSupport: setFetchSize(1000) on the streaming extract to cut JDBC round-trips
  (extraction-side, best-effort).

Operations dashboard:
- O2 By-source rollup moved ABOVE Executions in progress.
- O1 All totals clickable: tiles and every by-source number drill down to the
  matching feeds (new GET /api/overview/feeds, 10s server cache).
- O4 Per feed: last run (status + time) and last success, in the drill-down and
  next to each in-progress execution.
- O3 Executions list now shows Target as well as Source.
- G3 (observability): Resources panel (GET /api/overview/resources) with JVM heap
  used/available, processors, load average and running/queued/waiting/test counts.
Test runs are excluded from the production rollup and last-run stats.

Deferred to dedicated turns (design in .claude/2026-06-23-DESIGN-g1-identity-g3-
scheduler.md): G1 composite identity (feedId+targetId) and the G3 adaptive,
resource-gated parallel scheduler (config max-parallel-runs, default 1).

Compiles (98 classes). overview JS verified; no literal \n/\r; no unsafe [[ /[(.
