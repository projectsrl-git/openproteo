feat(bulk/ui): bulk recordBusinessDate fields, dashboard target column,
JSON/XML format button, light hover fix, sourceId in template list

Bulk create:
- Two new FEED CSV mapped columns, recordBusinessDate and
  recordBusinessDateFormat, injected into each generated workflow as
  variables (${recordBusinessDate} / ${recordBusinessDateFormat}).
- The template-workflow dropdown now shows each template's sourceId.

Dashboard:
- New Target column next to Source; targetId is also searchable.

Viewer / editor:
- A Format button pretty-prints JSON and XML for inline/unformatted
  files, both in the file viewer's edit mode and in the designer's
  direct-XML editor. Type is chosen by file extension, then content.

Light theme:
- Fixed button hover: the dark hover fills were hardcoded for the dark
  theme and made button text unreadable on hover under the light theme.

77 classes compile; all touched JS validates; no \n/\r literals in JS;
Thymeleaf-safe.
