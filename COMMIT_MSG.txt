Validate: businessDateNotBefore strictly after min, skip when unset

Adjust the businessDateNotBefore check semantics.

- The minimum is no longer defaulted to today. When the businessDateMin
  param/variable is absent the check is skipped (SKIP), not executed.
- Comparison is now strict: a business-date value equal to or earlier than
  businessDateMin is a violation; only a strictly later date passes
  (e.g. 20251231 vs min 20251231 -> violation; 20260101 -> pass).

Only engine/InternalSteps.java changes; the designer field and SAMPLE-06 wiring
are already in place. Compiles (103 classes); boundary verified.
