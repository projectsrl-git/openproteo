feat: campo targetId sul workflow (+ bulk multi-valore)

Aggiunto l'attributo targetId al workflow (destinazione), accanto a
sourceId, end-to-end.

- model WorkflowDef + DTO: campo targetId.
- parser: lettura attributo targetId dal <workflow>.
- writer: scrittura attributo targetId.
- engine: run.vars espone ${targetId}.
- designer: campo Target ID accanto a Source ID (modello/save/preview/
  load + ${targetId} nel cheat sheet).
- bulk: mappatura mapTargetId con la stessa sintassi template degli
  altri campi -> si possono indicare piu' destinazioni separate da
  virgola, es. {Dest A},{Dest B} -> targetId="ARCHA,ARCHB".

Verifiche:
- 74 classi compilate; TestTarget: {Dest A},{Dest B} ->
  targetId="ARCHA,ARCHB".
- designer/bulk JS validati; nessun \n/\r; Thymeleaf-safe.

Aggiornati README, CLAUDE, .claude/2026-06-16-targetid-field.md.
Redeploy WAR + Ctrl+F5.