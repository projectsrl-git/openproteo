Docs: bring USAGE.md up to date with the features added since the last pass

The online help (static/USAGE.md, rendered by the /docs page) was missing everything added
recently. New sections: step mode skip / on hold (pause) and how it differs from a manual gate;
mass-editing step mode from the Variables page; the Variables matrix (/matrix) with its
create-on-save cells, fill-down, Excel paste and "only columns that differ" filter;
currentDate/currentTs versus runDate/runTs; list indexing ${list[N]} with the loop-index idiom;
output data and run variables shown one value per line with a total; Operations filtering and the
current column set (Waiting appr., On hold, Other only when a feed lands in an unmapped status)
plus the weather legend and the tag badges; viewer row/line numbers and "go to"; the Aggregate tab
honouring the active filters; the standalone csv-viewer.html for testers; workflow duplication
copying the uploaded files; and what deleting a run does, including that the audit trail is kept.

Updated in place: the dequote executor bullet (logical records, the embeddedNewlines option and
the blankLinesRemoved / embeddedNewlinesRemoved outputs) and the Clear History section, which no
longer blocks production outright but asks for an explicit confirmation, can keep the most recent
run, and also runs from Operations on the current selection.

Note for future edits (recorded in the spec): the docs page renderer turns every source line into
its own paragraph, so prose must be written as one long line per paragraph and one line per
bullet; the added text follows that.

No code changes.
