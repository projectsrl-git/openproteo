bulk: post JSON, not form data, past Tomcat's 2 MB param cap

/bulk failed with a bare "Bad Request": Spring reported the required
parameter 'csv' as missing. The page posted form-urlencoded, and Tomcat
parses form parameters only up to maxPostSize (2 MB); past it every
parameter is dropped without an error. Inline dataschema/displayschema
JSON inflates about 1.75x under URL encoding, so roughly 1.2 MB of feeds
CSV was enough.

ApiController gains bulkCreateJson on the same path, consumes JSON, with
the same defaults as the form endpoint, delegating to bulkCreate. The page
sends JSON and shows HTTP <status> on a non-JSON error. The form endpoint
is unchanged for existing callers. Output of the generator is unchanged.

Verified: node --check on bulk.html inline JS, no literal \n/\r, no [[ or
[(, Java brace balance, imports present, inflation ratio measured.
Not verified: Maven build, standalone compile (no JDK in the sandbox),
end-to-end reproduction against Tomcat.
Details: .claude/2026-09-25-bulk-create-json-body.md
