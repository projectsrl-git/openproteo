ON HOLD UI: run page status/counts/PLAY + Operations column/PLAY (5.3.1-5.3.3)

Completes ON HOLD on top of F2 (engine + /resume endpoint).

5.3.1 Run page: ON_HOLD renders as a blue status chip (new .chip.ON_HOLD in
      app.css). A dedicated on-hold panel shows the held step and the partial
      success count "N of TOT steps successful so far"; partial outputs are the
      run.vars already listed under Variables. A "Continue (resume)" button
      (resumeRun -> POST /resume -> refresh) plus Stop; ON_HOLD counts as active so
      Stop stays available.
5.3.2 Operations: ON_HOLD shows as the blue chip in the grid; bucketFor maps
      ON_HOLD to a new "onhold" bucket rendered as an "On hold" rollup tile
      (clickable drill).
5.3.3 PLAY/CONTINUE: the run-page Continue button, and a "Continue" button on
      Operations grid rows whose last status is ON_HOLD (resumeRun(feedId,runId) ->
      POST /resume -> reload).

Docs: .claude/2026-07-15-onhold-ui-F3.md and CLAUDE.md.

Verify: run.html and overview.html pass node --check (no literal \n/\r, no unsafe
Thymeleaf). resumeRun mirrors the existing decide()/drillRunSeq patterns; the
backend change is one line in bucketFor. Full Maven build to be confirmed on deploy
(with F2).
