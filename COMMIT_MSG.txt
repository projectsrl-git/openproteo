Operations: show stop step, OPEN WORKFLOW + OPEN LAST RUN buttons

In the Operations drill-down feeds list:

- The Last run cell now shows, next to a FAILED/ABORTED status chip, the step
  where the run stopped (the failed step, or the step left running on abort).
- The single "open" action is renamed "open workflow" (still links to the
  workflow page).
- A new "open last run" action links straight to the last run's page
  (/run/{feedId}/{lastRunId}); shown only when a last run exists.

web/ApiController.java: /api/overview/feeds now returns lastRunId and failedStep
(computed from the most recent non-test run via a stoppedStepLabel helper).
templates/overview.html: render the stop step and the two action buttons.

Compiles (103 classes).
