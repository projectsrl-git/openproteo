PROD badge (home+ops); JSON/XML auto-prettify; CSV range search + sort

1) PROD flag now visible: red PROD badge on the dashboard feed id and on
   Operations (drill-down + executions). /api/overview/feeds returns production.

2) File viewer pretty-prints .json/.xml on open (read-only), via the existing
   formatter, without entering Edit; falls back to raw with a note if unparseable.

3) CSV viewer FROM/TO range search on selectable columns (dropdown + from/to +
   Add; multiple ranges AND, removable chips) and column sort (3.1: click header
   asc/desc/clear with arrow). Applied server-side in CsvService.page (streaming
   filter; sort buffers matching rows capped at 300k with a notice), combining
   with the text filter and virtual paging. New params fc/ff/ft + sortCol/sortDir;
   unchanged when unset.

Compiles (103 classes). CsvService self-test (numeric range, numeric vs
lexicographic sort, date range + secondary sort) passes. viewer.js/overview.html
JS verified.
