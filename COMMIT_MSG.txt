json2csv batch 4: USAGE.md, rendered rather than proof-read

USAGE.md gains a json2csv section and an entry in the executor list. 23
assertions green, bringing the total across four batches to 394. The feature
is complete as specified.

THE DOCUMENTATION IS RENDERED, NOT PROOF-READ

docs.html turns every source line into its own paragraph - there is no
soft-wrap merging. A paragraph wrapped at 100 columns in the file renders as
five separate paragraph blocks: invisible in an editor, obvious on the page,
and exactly the kind of defect that ships.

So tests/docs.js extracts render() from docs.html at build time and runs it
over the real USAGE.md. It asserts that no Markdown leaks outside code blocks,
that the section and its subsections arrived, and - the one that matters -
that no paragraph in the added section is a wrapped continuation of the one
above it.

89 wrapped paragraphs already exist elsewhere in USAGE.md. They are reported
by the harness and not fixed: rewrapping 700 lines of prose inside a patch
about an executor is the kind of unrelated churn that makes a diff
unreviewable. The assertion is scoped to what this batch added, and a second
assertion confirms the batch did not make the count worse.

A GREEN MUTATION FOUND A REAL GAP

Two mutations of USAGE.md came back green. Following the rule from batch 3 - a
green mutation is a claim about the mutation before it is a claim about the
suite - both were opened up rather than filed.

One was a bad mutation: changing ### to #### is not a defect. The other was a
real hole. The wrap check keyed on what the NEXT line started with, and
skipped it when that was a dash, reading it as a new bullet. A paragraph split
so that its continuation happened to begin with a dash therefore slipped past
both the source check and the rendered one.

The check now keys on the line that ends: a source line that does not end a
sentence, followed by more text, is a wrapped paragraph, whatever the
continuation happens to start with. That mutation is now caught, and realistic
wraps of a paragraph and of a bullet are caught by three assertions each.

WHAT THE SECTION DOCUMENTS

Beyond the parameter list: why [] is refused rather than read as [0]; why a
bracket-quoted key matters, since VM.CAP.DATE.CHARGE written bare parses as
four nested keys and silently resolves to nothing; why trying three input date
masks in order is safe here, and the warning that a fourth is not
automatically safe; that filesRead = rowsWritten is the number to check; that
an unmapped dataschema column is written empty rather than dropped; and that
an empty input directory is not an error.

The most useful line of the first-run list is that a high valuesMissing
usually means a path is mapped one level off, because a wrong path resolves to
absent rather than to an error - which is precisely why the catalogue shows
how often each attribute was actually seen.

VERIFIED / NOT VERIFIED

Verified: the real render() from docs.html run over the real USAGE.md, 23
assertions green; three mutations of the documentation caught after the check
was strengthened.

Not verified, and it is the same gap as every batch: mvn clean package, and a
run against real files. Nothing in four batches has been through Spring, a
WAR, Tomcat, or a single real document. On that first run, in order: that the
log prints one row per file with filesRead = rowsWritten = N; that
valuesMissing is not a multiple of the row count, which would mean a path
mapped one level off; and that the CSV header is the dataschema in dataschema
order, including the columns nothing maps.
