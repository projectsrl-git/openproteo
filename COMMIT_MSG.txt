Operations drill: add per-feed EDIT (open in designer) action

In the Operations detail rows, add a "✎ edit" link next to "open workflow" /
"open last run" pointing at the existing GET /designer/{feedId} route, so a
failing feed can be opened straight in the designer to fix it.

Docs: .claude/2026-07-14-operations-drill-edit-button.md and CLAUDE.md updated.

Frontend only; no endpoint/model change. overview.html passes node --check; no
literal \n/\r; no unsafe Thymeleaf inline.
