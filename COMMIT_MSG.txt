Docs: record viewer line numbers, dequote blank lines and Waiting-approval in CLAUDE.md

The previous patch deliberately left CLAUDE.md untouched so that it could be applied in any
order with the Variables-matrix patch (both appended at the end of that file, and two
EOF-append hunks conflict). Both are now on main, so this documentation-only change adds the
missing CLAUDE.md entry for:

- the viewer's row/line numbers and "go to" box (CSV gutter folded into rowWidthPx, TXT go-to,
  JSON/XML rendered through the line-numbered virtual list, shared gotoBox/renderLines helpers
  and the .vnum/.vwr-goto/.hl styles);
- the dequote executor skipping blank lines and reporting blankLinesRemoved, while keeping
  delimiter-only rows;
- WAITING_APPROVAL becoming its own bucket with the "Waiting approval" tile and the
  "Waiting appr." by-source column after Running, and the Other column being hidden unless a
  feed really lands in an unmapped status so the columns keep reconciling with Total.

No code changes.
