diff executor (Batch 4): cross-workflow file picker + run correlation

Practical core of the design's §7 (cross-workflow reconciliation) for this
codebase. Applies on top of Batch 1-3 (all on main).

A feed's produced files live in stable directories overwritten each run, so a
stable absolute path already resolves to the latest run's output. Cross-workflow
referencing therefore reduces to picking another workflow's file (store its
absolute path) and stamping the file's produced time in the report.

- Backend: new GET /api/workflows/catalog -> {ok, workflows:[{feedId,name}]} from
  registry.all(), for the workflow dropdown. The file dropdown reuses the existing
  GET /api/workflows/{feedId}/files (files[].path + dir).
- Designer: under File A and File B, a "...or from a workflow" row with a workflow
  select (lazy-loaded from the catalog on focus) and a file select (loaded from
  that workflow's files). Picking a file writes the absolute path (dir + relative,
  backslashes normalised) into the field and the fileA/fileB param. Free-text
  paths still work.
- Run correlation: every mode's report now stamps
  "Sources produced: A @ <mtime>, B @ <mtime>" in the config echo (the produced
  instant of each compared file; given stable overwritten paths, mtime is the
  faithful produced-instance stamp).

Docs: .claude/2026-07-14-diff-executor-batch4-crossworkflow.md and CLAUDE.md.

Verify: the actual updated runDiffKey was run standalone -- the report shows the
Sources stamp and the exit code is correct. designer.html passes node --check; no
literal \n/\r; no unsafe Thymeleaf. The catalog endpoint mirrors the existing
/active and /queue GETs. The designer's async picker flow could not be live-tested
in the chat sandbox (no running app); it mirrors the existing xlsx-sheets
async-select pattern. Full Maven build to be confirmed on deploy.
