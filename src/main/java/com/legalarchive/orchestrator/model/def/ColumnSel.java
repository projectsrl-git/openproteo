package com.legalarchive.orchestrator.model.def;

/** xlsx2csv: one selected column — source (header name or letter) and optional output rename. */
public class ColumnSel {
    public String src;
    public String as;
}
